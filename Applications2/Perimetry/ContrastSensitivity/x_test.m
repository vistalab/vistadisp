% Template for Tests% April 4,2004% Claudioclear all;close all;clear mex;perimetrydlg;global display%Display Parameters%Change here any display parameter if neededdisplay = loadDisplayParams('screenNumber', 1);display.distance=20;%display = usePredefinedDisplay('screenNumber', 1, 'resolution', '1024x768, 75Hz', 'cmapDepth', 10);display = openScreen(display);% Set your Stimulus parameters% All position in this format [vertical, horizontal]stimParams.position=[300,300];stimParams.duration = 0.2;	% in secondsstimParams.contrast = 0.8;%stimParams.image = blkball(display.distance,display,18,15);%stimParams.image = whiteball(display.distance,display,18,15);stimParams.image = gabor(display.distance,display,12,8,4);% Set your  Test ParameterstestParams.alternativeVarName = 'position';testParams.subject = name;testParams.group = group;testParams.sessionNum = session;testParams.imgfixpoint=fixpoint(20,10);testParams.fixloc = [300,400];%testParams.alternativeVarValues = [200,100;300,400];testParams.alternativeVarValues = [200,300;500,500];%testParams.alternativeVarValues = perilocations(display,testParams.fixloc(1),testParams.fixloc(2));testParams.expname = {'Perimetry'};testParams.contrastscale =logspace(-2.2,0,5);% The starting contrast used in task1testParams.medcontrastindex = 3;testParams.keyyes='1';testParams.keyno='2';testParams.keystop='q';testParams.maxtrials=500;% units in number of trials unitstestParams.catchcycle=9;welcomestring=['Welcome, press ' testParams.keyyes ' for "yes", ' testParams.keyno ' for "no", ' testParams.keystop ' to quit'];pressKey2Begin(display,welcomestring);showfix;clear x1 y1 x2 y2 z box_title entries;datetoday =date;timestart =clock;timestartt=[num2str(timestart(4)),':',num2str(timestart(5)),':',num2str(round(timestart(6)))];% Call here Main routine%timestop =clock;timesstopt=[num2str(timestop(4)),':',num2str(timestop(5)),':',num2str(round(timestop(6)))];eval(['save ' filename]);screen('CloseAll');clear all;close all;
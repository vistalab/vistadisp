function pth = locImageDB;% imageDB - localizer image database directory%% pth = locImageDB%% returns the path for the image database on the local computer. % Change this for different computers. This allows more flexibility % in porting scripts from one place to another. First put in use% 10/03, for rory's masking experiments (starting w/ expt 4).%% 10/03 ras.% 04/2006 SOD: made relative to loc % 01/2009 RAS: renamed from imageDB to locImageDB. This prevents conflicts% with other functions named imageDB which may exist in local stimulus% code.% Commented out -- this is for Mac OS 9 on the stimulus machine:%pth = fullfile(machinename,'ImageDB','JPG');% This is for more modern machines connected to the RAIDpth = fullfile(fileparts(fileparts(which('loc'))),'JPG');return;
function today = getToday;% ported from Junjie's code: this provides a six-digit (month, day, year) % string indicating today's date.% ras, 05/28/08.today = datestr(now,2);today = today([1,2,4,5,7,8]);%mm-dd-yycurtime = datestr(now,16);if strcmp(curtime(end-1:end),'AM') % if AM shift 1 day earlier	% Bet on you don't scan at midnight of first day of a month!	tmp = int2str(str2num(today(2:end))-100 + 100000);    today(2:end) = tmp(2:end);endreturn